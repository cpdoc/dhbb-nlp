The folder PLNbase contains all dhbb-nlp raw texts segmented with PLNbase library.

One can generate their own files by copying "remove_spaces.py" and "segment.pl" into dhbb-nlp folder and runing the following commands in this specific order:

python3 remove_spaces.py
perl segment.pl
